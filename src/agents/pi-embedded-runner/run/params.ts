import type { OpenClawConfig } from "../../config/config.js";
import type { MessageChannel } from "../../message-channels/types.js";
import type { SkillManifest } from "../../skills/types.js";
import type { EmbeddedPiRunResult } from "../types.js";
import type { ThinkLevel } from "../../auto-reply/thinking.js";

export type RunEmbeddedPiAgentParams = {
  sessionId: string;
  sessionKey?: string;
  trigger?: string;
  memoryFlushWritePath?: string;
  messageChannel?: MessageChannel;
  messageProvider?: string;
  agentAccountId?: string;
  messageTo?: string;
  messageThreadId?: string;
  groupId?: string;
  groupChannel?: string;
  groupSpace?: string;
  spawnedBy?: string;
  senderId?: string;
  senderName?: string;
  senderUsername?: string;
  senderE164?: string;
  senderIsOwner?: boolean;
  currentChannelId?: string;
  currentThreadTs?: string;
  currentMessageId?: string;
  replyToMode?: string;
  hasRepliedRef?: { current: boolean };
  sessionFile?: string;
  workspaceDir?: string;
  agentDir?: string;
  config: OpenClawConfig;
  contextEngine: any;
  contextTokenBudget?: number;
  skillsSnapshot?: SkillManifest[];
  prompt: string;
  images?: Array<{ url: string; alt?: string }>;
  disableTools?: boolean;
  provider: string;
  model: string;
  modelId: string;
  authProfileId?: string;
  authProfileIdSource?: string;
  authStorage: any;
  modelRegistry: any;
  agentId?: string;
  legacyBeforeAgentStartResult?: any;
  thinkLevel?: ThinkLevel;
  fastMode?: boolean;
  verboseLevel?: number;
  reasoningLevel?: string;
  toolResultFormat?: "plain" | "markdown";
  execOverrides?: any;
  bashElevated?: boolean;
  timeoutMs?: number;
  runId?: string;
  abortSignal?: AbortSignal;
  shouldEmitToolResult?: boolean;
  shouldEmitToolOutput?: boolean;
  onPartialReply?: (text: string) => void;
  onAssistantMessageStart?: () => void;
  onBlockReply?: (text: string) => void;
  onBlockReplyFlush?: () => void;
  blockReplyBreak?: string;
  blockReplyChunking?: boolean;
  onReasoningStream?: (text: string) => void;
  onReasoningEnd?: () => void;
  onToolResult?: (result: any) => void;
  onAgentEvent?: (event: any) => void;
  extraSystemPrompt?: string;
  inputProvenance?: string;
  streamParams?: any;
  ownerNumbers?: string[];
  enforceFinalTag?: boolean;
  bootstrapPromptWarningSignaturesSeen?: string[];
  bootstrapPromptWarningSignature?: string;
  enqueue?: (task: () => Promise<any>, opts?: any) => Promise<any>;
};
